# Insights — server/

Non-obvious findings accumulated by past sessions in this scope. Read before non-trivial work
here. Every entry should be actionable cold: a session that reads it without any other context
should know what to do or avoid.

Append with the `engineering-insights` skill (`/engineering-insights`), never by hand — the skill
dates entries, keeps the section order, and refuses duplicates. Existing entries are append-only:
correct them with a dated note below, never by rewriting.

Entry format: `` - `YYYY-MM-DD` — finding → evidence ``

## What Works

- `2026-08-01` — The error-handler contract has a hermetic regression guard that needs no Postgres: `routes-smoke` builds the app with `buildApp({ config })` and asserts 422 + `error.code === "validation_error"` via `app.inject`, so changes to the zod branches can be verified in seconds without testcontainers → `cd server && pnpm test routes-smoke` (`test/routes-smoke.test.ts:56`)
- `2026-08-02` — When a feature spans route → bus → executor → repo, test the WIRING, not the contract: cancellation had one passing engine-contract test (a throwing callback stops the loop) and four defects, every one of them in a layer that test never touched. The contract was the only thing not broken → `reviewer-core/test/run.test.ts:91`
- `2026-08-05` — There is no hermetic-only test script in server/package.json (`test` is a bare `vitest run`), so run one side of the documented .it. split by globbing it out: `pnpm test --exclude '**/*.it.test.ts'` gives 21 files / 124 tests in ~2s with no Docker, which is the fast full-confidence check for any change that does not touch DB code → server/package.json scripts.test, run during Task 4 on 2026-08-05
- `2026-08-06` — Making a DI container method's argument required with no default (e.g. Container.github(githubTokenId: string | null)) turns pnpm typecheck into a call-site finder for a signature change; it caught a call site at settings/routes.ts:87 that an implementation plan's explicit list of 5 sites had missed → server/src/modules/settings/routes.ts:87, server/src/platform/container.ts
- `2026-08-20` — A real-Postgres BEFORE trigger that raises an exception (created/dropped inside the test) is the mock-free way to prove repository atomicity: it RED-failed the pre-transaction deleteAgentRun (review deleted, run row intact) and catches any future de-transactioning — verified by mutation on all three transactional helpers → server/test/atomic-writes.it.test.ts, 2026-08-20
- `2026-08-20` — fastify-type-provider-zod response schemas actively STRIP undeclared fields at serialization — a handler deliberately leaking token_value never reached the wire once response: {200: GitHubToken} was set, proven by two-step mutation (leak passes with schema, fails without) — so response schemas are a working secret-leak guard, not just validation → server/test/response-schemas.it.test.ts 'secret-leak guard', 2026-08-20

## What Doesn't Work

- `2026-08-01` — Debugging an in-flight review under `pnpm dev` does not work: the boot reaper is awaited before the server listens and unconditionally flips EVERY row with `status = running` to `failed`, so a single file save under `tsx watch` reloads the process and silently kills the run being watched. The run just becomes `failed` with no error event and no stack — the only trace is the log line `reaped stale running agent_runs on boot`. Do not edit server files while a run is in flight; for long runs start the API without watch → `server/src/app.ts:81`, unconditional `where(eq(t.agentRuns.status, "running"))` at `server/src/modules/reviews/repository/run.repo.ts:104` → CLAUDE.md
- `2026-08-01` — Any failing background job takes down the whole API: `JobRunner.enqueue` marks the row `failed` and then rethrows into the `done` promise, and every caller in `modules/repos/service.ts` ignores `done`, so the rejection is unhandled and Node exits. Symptom: the process dies with a stack trace and no request in flight, while the `jobs` row already says `failed` — which is why the rethrow buys nothing on the fire-and-forget path → `src/platform/jobs.ts:96`
- `2026-08-01` — Authenticating a clone by embedding the PAT in the URL (`withGitHubToken`) leaks it: on failure simple-git echoes the whole argv, so the token lands in plaintext in the API log and in any pasted stack trace. A 403 from a repo the PAT lacks access to is enough to trigger it → `src/modules/repos/service.ts:54`
- `2026-08-02` — The reviews integration suite made REAL paid OpenRouter calls: `appWith` stubbed only the `openai` provider while the seeded agents are openrouter/deepseek, so every `all: true` review resolved a live provider. Symptom is a run row with genuine tokens that no mock produced; stubbing all three provider ids cut the file from 55s to 4.8s — the runtime WAS the network → `server/test/reviews.it.test.ts:113`
- `2026-08-02` — Rethrowing from a fire-and-forget p-queue job so 'callers can see the failure' — no caller awaits done, so every job failure became an unobserved rejection and exited the Node process; pre-observe with done.catch(() => undefined) and the rethrow still reaches anyone who does await → server/src/platform/jobs.ts:106, server/test/jobs-crash.test.ts
- `2026-08-06` — Deleting LocalSecretsProvider's GITHUB_TOKEN special case does not close the GitHub env-fallback: the generic final line `return this.env[key as string]` reads process.env for ANY key, so repos/service.ts's still-untouched `secrets.get(GITHUB_TOKEN_SECRET)` (literal key 'GITHUB_TOKEN') keeps reading a bare GITHUB_TOKEN from server/.env exactly as before — the special case only ever added the extra GITHUB_PAT fallback on top of the same env read. The new per-repo path (GITHUB_TOKEN:<id> keys) has no fallback because no .env file defines that key name; the legacy clone-auth path does, until Task 8 replaces GITHUB_TOKEN_SECRET with CloneJobPayload.githubTokenId → server/src/adapters/secrets/local.ts:37-40, server/src/modules/repos/service.ts:53, Task 6 2026-08-06
- `2026-08-06` — Asserting a route's token_missing 422 in a suite that injects `overrides.github` — Container.github() returns the injected client for ANY id, including null, so the mock masks exactly the branch under test and the route answers 200. Build a second app WITHOUT the github override for that one case: `container.github(null)` then throws MissingTokenError before any Octokit is constructed, so the test stays hermetic and exercises the production path → server/src/platform/container.ts github(), server/test/repos-github-token.it.test.ts "POST /repos/:id/poll on a repo with no token", server/test/container-github-cache.test.ts "an injected override wins over resolution, with any id"
- `2026-08-08` — The run SSE, cancel, and trace routes resolve request context but discard workspaceId: /events subscribes the global RunBus by runId, cancel updates agent_runs by id/status, and trace selects run_traces by runId, so a multi-workspace AuthProvider cannot prevent cross-workspace log reads or cancellation. Thread workspaceId through service/repository, verify run ownership before bus access, and cover all three routes with a two-workspace regression test → server/src/modules/reviews/routes.ts:48-53,113-125; server/src/modules/reviews/service.ts:85-89,176-177; server/src/modules/reviews/repository/run.repo.ts:93-100,190-192
- `2026-08-20` — Evicting RunBus terminal flags with the heavy state: agents for one PR run SEQUENTIALLY, so a cancelled run can reach the executor's isCancelled() checkpoint long after any grace window — evicting 'cancelled' re-created the cleared-flag billing bug on a 5-minute delay, and evicting 'completed' made a late SSE subscriber await a 'done' that never fires; evict only buffers/seq/emitters/controllers and keep the two uuid-string Sets forever → server/src/platform/sse.ts evict(), caught by branch review after sse-lifecycle tests had ASSERTED the buggy release as intended, 2026-08-20

## Codebase Patterns

- `2026-08-01` — Validation failures answer 422 here, never 400, and the error handler has two independent zod branches: `hasZodFastifySchemaValidationErrors` at :118 catches schema-first failures from the type provider (`details` comes from `err.validation`), while the shape-checked branch at :143 catches service-level `.parse()` calls and routes not yet on `schema.body` (`details` comes from `.issues ?? .errors`). Which branch produced the 422 tells you where the parse happened, so fixing one path does not cover the other → `src/app.ts:116`
- `2026-08-01` — Run-event aggregation lives in the client on purpose — do not add a server-side endpoint that returns an aggregated/snapshot view of a run event log. The replay-first RunBus (buffer kept after completion, so late subscribers get full history) plus client-side accumulation already covers it; full reasoning in `client/INSIGHTS.md` under 2026-08-01 → `server/src/platform/sse.ts:43`
- `2026-08-02` — Cost lives on `agent_runs`, and `reviews.run_id` is the only link from a review back to its usage — the verdict banner reads cost by joining through it client-side rather than adding a field to `ReviewRecord`, because the timeline on the same page has already fetched the run list → `client/src/app/repos/[repoId]/pulls/[number]/_components/ReviewRunAccordion/ReviewRunAccordion.tsx:57` ×2 (2026-08-03: severity badges reuse the same join, PR #8)
- `2026-08-05` — The API derives its CORS allow-origin from WEB_PORT — `webOrigin: http://localhost:${WEB_PORT}` at server/src/platform/config.ts:82, consumed by `app.register(cors, { origin: [config.webOrigin] })` at src/app.ts:95 — so running the client on a non-default port requires setting WEB_PORT on the API process too, not just the client's -p flag, or every browser call fails CORS while curl still works → hit while booting web on :3010 with API_PORT=3011 on 2026-08-05
- `2026-08-05` — The testcontainers fixture hands back `handle`, not a bare db: `PgFixture` is `{ container, handle, url, stop }` and `handle: DbHandle` carries BOTH `.db` (drizzle, for typed inserts) and `.sql` (raw postgres.js tag, which is how table-existence and pg_extension assertions get written) — so it is `pg.handle.db`, never `pg.db`, and any plan sketch written against `pg.db` fails at the first insert → server/test/helpers/pg.ts:13, used both ways in server/test/integration.it.test.ts:33 and :47
- `2026-08-05` — A regression test guarding a DELETED fallback needs a deliberately over-generous fake, or it proves nothing: the fake SecretsProvider in the token-resolver test also serves from a supplied env map, so if resolveGitHubToken ever again consults a bare GITHUB_TOKEN it gets a real value back and the test fails. The env parameter looks unused and dead — removing it as a simplification silently guts the guard while leaving it green → server/test/github-token-resolver.test.ts:24 fakeSecrets, guard test at :58 ~~superseded~~ see 2026-08-05 (entry below)
- `2026-08-05` — Correcting the 2026-08-05 entry above, which overclaimed: a fake-mediated env map guards only a PROVIDER-mediated fallback (`secrets.get('GITHUB_TOKEN')`) — a direct `process.env.GITHUB_TOKEN` read inside the code under test bypasses the fake entirely and the assertion then depends on whatever the real process has set. Guard the environment shape by stubbing the REAL env with `vi.stubEnv` + `afterEach(vi.unstubAllEnvs)` and keep the fake env-free; both shapes now have a test. Verified by mutation: adding `?? process.env.GITHUB_TOKEN` to the resolver fails the stubbed test with 'promise resolved ghp_env' → server/test/github-token-resolver.test.ts:61 (stubbed env) and :74 (bare provider key)
- `2026-08-06` — Container's constructor is (config, db, overrides) — hermetic unit tests that only exercise a non-db path (e.g. container.github()) can pass `{} as Db` for db instead of a real postgres-js handle; Container never touches this.db unless a route/repository does → server/test/container-github-cache.test.ts
- `2026-08-06` — Container.overrides is a private constructor field on purpose — a service that needs ONE injected adapter to validate a raw value (GitHubTokenService validating a raw PAT via the test-injected GitHubClient before any token id exists) gets a narrow single-purpose getter (Container.overriddenGithub) rather than the whole field being made public, so a future consumer can't reach overrides.secrets/llm/etc through the same door → server/src/platform/container.ts overriddenGithub getter, server/src/modules/github-tokens/service.ts clientFor(), Task 7 of per-repo-github-tokens
- `2026-08-06` — The bare-GITHUB_TOKEN env fallback is closed by ABSENCE OF A LOOKUP, not by a guard: LocalSecretsProvider.get still ends in `return this.env[key as string]`, so any future `secrets.get('GITHUB_TOKEN')` silently re-authenticates clones from server/.env. The only enforceable regression test is therefore a source-text scan of server/src for the quoted literal (vendor/shared skipped — SecretKey's union arm is a type and cannot perform a lookup); the per-token keys spell it `GITHUB_TOKEN:${id}` so the ':' keeps them out of the match → server/test/no-env-github-token.test.ts, verified to fail by re-adding GITHUB_TOKEN_SECRET to server/src/modules/repos/constants.ts
- `2026-08-06` — server/src/db/seed.ts now inserts one github_tokens row (label 'demo', no PAT value → configured:false) and points the demo repo at it — any it.test.ts in this file that fetches github-tokens rows by array position or asserts an exact array length assumes an empty-token workspace, which is no longer true. Two assertions in github-tokens-routes.it.test.ts broke this way: 'lists tokens with repo_count' expected length 1 (now 2), and a later test grabbed row[0] by createdAt assuming it was its own first-created 'work' token, but the seeded 'demo' token is older and silently became row[0] instead — fixed by finding rows by label → server/test/github-tokens-routes.it.test.ts, Task 12 of per-repo-github-tokens 2026-08-06
- `2026-08-20` — A two-workspace it test that needs a status='running' agent_runs fixture must insert it AFTER buildApp — every buildApp boots the reaper, which unconditionally flips ALL running rows to failed, so a pre-boot fixture silently arrives failed and any 'stays running' assertion tests nothing → server/test/workspace-scope.it.test.ts beforeAll ordering, 2026-08-20

## Tool & Library Notes

- `2026-08-03` — git strips URL userinfo from its own error output (transport_anonymize_url) — a tokened clone URL fails with "fatal: unable to access 'https://github.com/...'" with credentials already removed, so a reviewer claim that the PAT leaks via git stderr into jobs.error did not reproduce; the scrub added in platform/jobs.ts redactUrlCredentials() is defense-in-depth for non-git job kinds, not a fix for git → verified empirically 2026-08-03 against a live 403 row and a synthetic bad-token clone (PR #7)
- `2026-08-06` — Interpolating a drizzle Column object into a sql template tag does NOT qualify it with its table name — it renders as a bare quoted column name. Inside a correlated subquery whose own FROM table shares a column name with the outer table (near-guaranteed for id), the bare reference silently binds to the wrong table with no error and no warning, just a wrong number. Any correlated subquery here needs the outer-scope column written as an explicit qualified literal (schema-name.column), never a bare ${outerTable.column} interpolation → server/src/modules/github-tokens/repository.ts repoCount, verified by printing .toSQL() and reproducing against a throwaway pg container, Task 12 2026-08-06
- `2026-08-08` — dependency-cruiser@17.4.3 matches `to.path` against normalized module paths, so a generic `(?:^|/)src/` target intended to mean the server also matches sibling-package paths like `../reviewer-core/src/`; use `^src/` when the cruise runs from `server/` and the rule must match only server source → raw Task 7 cruise fell from 47 to 31 violations after removing 16 false reviewer-core-internal edges in `server/.dependency-cruiser.cjs` — ~~superseded~~ see 2026-08-08 correction below
- `2026-08-08` — Correcting the 2026-08-08 dependency-cruiser entry above, which was incomplete: dependency-cruiser@17.4.3 matches forbidden-rule `to.path` against `dependency.resolved` (not the unresolved `module` specifier), so package rules need resolved `(?:^|/)node_modules/<package>(?:/|$)` patterns; `options.preserveSymlinks: true` stabilizes pnpm dependencies at direct node_modules paths, Node builtins resolve without the `node:` prefix, and the `@devdigest/shared` alias resolves to `^src/vendor/shared/` → `node_modules/dependency-cruiser/src/validate/matchers.mjs:88-90`, verified by `depcruise --output-type json` against Task 7 fixtures and production
- `2026-08-08` — dependency-cruiser@17.4.3 with `--output-type json` exits 0 even when `summary.violations` contains error-severity violations, unlike the `err` reporter whose exit status reflects failures; semantic gate tests must parse and compare `summary.violations` instead of using the JSON process status as the violation signal → `server/test/architecture-gate.test.ts` baseline-integrity test, observed when the expanded Task 7 RED suite initially had one harness-only failure
- `2026-08-20` — The legacy-* dependency-cruiser rules match FILE NAMES (routes.ts, service.ts, repository.ts|/) with a frozen baseline, so restructuring a legacy module to routes→service→repository creates NEW violations while module helper files under other names (pr-files.ts, store.ts, detail.ts) carry persistence edges without tripping any rule — the gate constrains naming, not just layering → server/.dependency-cruiser.cjs forbidden rules, atomic-writes placement 2026-08-20
- `2026-08-20` — Adding a response schema whose shared contract carries .default()s is NOT behavior-neutral: serialization injects the defaults for absent keys, so a sparse settings row now answers with theme:'dark' etc. even if never PUT — check contracts for defaults before wiring response: {200: Contract} → server/src/modules/settings/routes.ts GET/PUT + Settings contract, 2026-08-20

## Recurring Errors & Fixes

- `2026-08-01` — A 500 with a generic `internal_error` body where a route should have answered 422 `validation_error` on a malformed payload means a ZodError reached the handler and failed every branch: it was raised by a service-level `.parse()` inside `src/vendor/shared/`, so `err instanceof z.ZodError` was false against the server’s own zod copy. The shape check (`name === "ZodError"` plus an `issues` or `errors` array) is the branch that actually catches it → `src/app.ts:138` → CLAUDE.md
- `2026-08-01` — A 500 with a generic `internal_error` body from a newly added route, with `relation "agent_runs" does not exist` (Postgres 42P01) underneath it in the API log, means migrations were never applied to the DB this API is pointed at. Nothing applies them on boot and `./scripts/dev.sh` does not either, so any route touching a table added by an unapplied migration 500s on its first request — the endpoint code is fine → fix with `cd server && pnpm db:migrate`, then retry the request
- `2026-08-02` — A job row stuck 'running' with no matching git/child process after a tsx-watch reload is an orphan: the in-memory p-queue died with the old process but the DB row survives, and anything keying off jobs.status (refresh dedupe) pins itself to a job that will never finish → server/src/platform/jobs.ts reapOrphans(), wired in app.ts boot; found only by live verification after all tests were green (PR #7)
- `2026-08-05` — A drizzle TypeError "Cannot read properties of undefined (reading 'Symbol(drizzle:Columns)')" thrown from PgInsertBuilder.values means the table you referenced as t.<name> is not exported by the db/schema.ts barrel — the symbol read fails on undefined before any SQL is built, so it looks like a drizzle/container problem rather than a missing export. Adding a table needs THREE barrel edits in the schema.ts FILE (there is no schema/index.ts): the export * line, the named import, and the entry in the exported schema object used for drizzle client typing → hit adding githubTokens, server/src/db/schema.ts, commit cdc7b9d
- `2026-08-05` — `reviews.it.test.ts > cancel stops the in-flight request AND the agents that have not started` fails as `expected 2 to be 1` under full-suite load but passes 8/8 in isolation — it is timing-flaky, not a regression: it counts provider calls made before an abort lands, so any CPU contention from parallel test files lets a second agent start. Re-run the file alone before debugging a change that did not touch the cancel path → server/test/reviews.it.test.ts:386, observed 2026-08-05 during Task 2 (schema-only change), full suite green 148/148 on immediate re-run ×2 (2026-08-20: same single failure in two consecutive full-suite runs, passed alongside workspace-scope.it in isolation)
- `2026-08-06` — POST /github-tokens answering 422/502 "Failed to store the token value" against the REAL LocalSecretsProvider (while every test passed) is an unbound-method bug, not a filesystem problem: `const set = this.container.secrets.set; await set(k, v)` detaches the method, so `this` is undefined inside the class and `this.load()` throws. Every test injected an object-literal secrets mock whose closures never touch `this`, so nothing caught it. Call optional provider methods through the object (`this.container.secrets.set(...)`) and put at least one test on the real adapter → server/src/modules/github-tokens/service.ts:162, found by server/test/repos-github-token.it.test.ts building an app with LocalSecretsProvider
- `2026-08-06` — GET /github-tokens repo_count reads 0 for every token even when repos point at it means the correlated subquery's outer-column reference is unqualified: GitHubTokenRepository.list()'s repoCount sql template interpolated ${t.githubTokens.id} inside a subquery FROM'd on repos — drizzle renders an interpolated Column as a bare column name with no table prefix, and repos has its own id column, so Postgres's inner-scope-shadows-outer rule bound the bare "id" to repos.id, making the WHERE clause repos.github_token_id = repos.id (never true). repoCountFor(id) (used by DELETE's orphaned_repos) was unaffected — it compares to a literal id via eq(), not another column. Fix: qualify the outer side with the outer table's actual unaliased SQL name as literal text, e.g. "= github_tokens.id" → server/src/modules/github-tokens/repository.ts repoCount in list(), caught by e2e/specs/08-github-tokens.flow.json asserting '1 repo' text, Task 12 of per-repo-github-tokens 2026-08-06
- `2026-08-06` — RepoWithToken.github_token_id alone is not enough to know a repo can reach GitHub — a repo can be ASSIGNED to a token whose stored value is absent (deleted, or never given a PAT, e.g. the seeded demo token: server/db/seed.ts), and every existing signal (RepoTokenBadge, the broken banner, MissingTokenError's old default message) keyed off the id alone, so that state looked healthy. Fixed by adding RepoWithToken.github_token_configured: z.boolean(), resolved server-side via resolveGitHubToken + catch MissingTokenError → false, and gating UI on !github_token_id || !github_token_configured → server/src/modules/repos/service.ts isTokenConfigured(), server/src/modules/repos/helpers.ts toRepoWithTokenDto, final review pass on feat/per-repo-github-tokens 2026-08-06
- `2026-08-06` — GitHubTokenRepository.repoCountFor(id) took no workspaceId, unlike every sibling query in this file — filtered only repos.githubTokenId = id, so a cross-workspace id would still return a real count (not exploitable today only because both call sites, remove() and patch(), already validate ownership before calling it). Changed to repoCountFor(workspaceId, id), filtering repos.workspaceId too → server/src/modules/github-tokens/repository.ts, final review pass on feat/per-repo-github-tokens 2026-08-06

## Session Notes

### 2026-08-20

- The 2026-08-08 run SSE/cancel/trace workspaceId gap is CLOSED (plus repo-intel index-state/resync, finding accept/dismiss UPDATE predicates, job-payload Zod parses, RunTrace parse-on-read) — guarded by test/workspace-scope.it.test.ts and test/job-payload-validation.test.ts; do not re-fix → server/src/modules/reviews/{routes,service}.ts, 2026-08-20

## Open Questions

- `2026-08-01` — Is the leading `err instanceof z.ZodError` at `src/app.ts:140` worth keeping now that the shape check subsumes it? It is provably dead for anything raised inside `src/vendor/shared/`, but still the only branch that would catch a subclassed or renamed ZodError from server-local code; dropping it makes the handler depend entirely on the string `"ZodError"` surviving in `err.name`
